%h2 Photos
= link_to "Add New Photo", new_admin_photo_path, :class => "small-button"
.section
  %table.datatable
    %thead
      %tr
        %th Name
        %th Tags
    %tbody
      - @photo.each do |photo|
        %tr
          %td= link_to photo.name, [:admin, photo]
          %td= photo.tags.collect {|x| x.name}.join(", ")
